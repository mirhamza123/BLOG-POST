import {
  require_react
} from "./chunk-RLJ2RCJQ.js";
import {
  __export,
  __publicField,
  __toESM
} from "./chunk-DC5AMYBS.js";

// node_modules/@contentful/rich-text-react-renderer/dist/rich-text-react-renderer.esm.js
var import_react = __toESM(require_react());

// node_modules/@contentful/rich-text-types/dist/esm/blocks.mjs
var BLOCKS = function(BLOCKS2) {
  BLOCKS2["DOCUMENT"] = "document";
  BLOCKS2["PARAGRAPH"] = "paragraph";
  BLOCKS2["HEADING_1"] = "heading-1";
  BLOCKS2["HEADING_2"] = "heading-2";
  BLOCKS2["HEADING_3"] = "heading-3";
  BLOCKS2["HEADING_4"] = "heading-4";
  BLOCKS2["HEADING_5"] = "heading-5";
  BLOCKS2["HEADING_6"] = "heading-6";
  BLOCKS2["OL_LIST"] = "ordered-list";
  BLOCKS2["UL_LIST"] = "unordered-list";
  BLOCKS2["LIST_ITEM"] = "list-item";
  BLOCKS2["HR"] = "hr";
  BLOCKS2["QUOTE"] = "blockquote";
  BLOCKS2["EMBEDDED_ENTRY"] = "embedded-entry-block";
  BLOCKS2["EMBEDDED_ASSET"] = "embedded-asset-block";
  BLOCKS2["EMBEDDED_RESOURCE"] = "embedded-resource-block";
  BLOCKS2["TABLE"] = "table";
  BLOCKS2["TABLE_ROW"] = "table-row";
  BLOCKS2["TABLE_CELL"] = "table-cell";
  BLOCKS2["TABLE_HEADER_CELL"] = "table-header-cell";
  return BLOCKS2;
}({});

// node_modules/@contentful/rich-text-types/dist/esm/inlines.mjs
var INLINES = function(INLINES2) {
  INLINES2["ASSET_HYPERLINK"] = "asset-hyperlink";
  INLINES2["EMBEDDED_ENTRY"] = "embedded-entry-inline";
  INLINES2["EMBEDDED_RESOURCE"] = "embedded-resource-inline";
  INLINES2["ENTRY_HYPERLINK"] = "entry-hyperlink";
  INLINES2["HYPERLINK"] = "hyperlink";
  INLINES2["RESOURCE_HYPERLINK"] = "resource-hyperlink";
  return INLINES2;
}({});

// node_modules/@contentful/rich-text-types/dist/esm/marks.mjs
var MARKS = function(MARKS2) {
  MARKS2["BOLD"] = "bold";
  MARKS2["ITALIC"] = "italic";
  MARKS2["UNDERLINE"] = "underline";
  MARKS2["CODE"] = "code";
  MARKS2["SUPERSCRIPT"] = "superscript";
  MARKS2["SUBSCRIPT"] = "subscript";
  MARKS2["STRIKETHROUGH"] = "strikethrough";
  return MARKS2;
}({});

// node_modules/@contentful/rich-text-types/dist/esm/schemaConstraints.mjs
var TOP_LEVEL_BLOCKS = [
  BLOCKS.PARAGRAPH,
  BLOCKS.HEADING_1,
  BLOCKS.HEADING_2,
  BLOCKS.HEADING_3,
  BLOCKS.HEADING_4,
  BLOCKS.HEADING_5,
  BLOCKS.HEADING_6,
  BLOCKS.OL_LIST,
  BLOCKS.UL_LIST,
  BLOCKS.HR,
  BLOCKS.QUOTE,
  BLOCKS.EMBEDDED_ENTRY,
  BLOCKS.EMBEDDED_ASSET,
  BLOCKS.EMBEDDED_RESOURCE,
  BLOCKS.TABLE
];
var LIST_ITEM_BLOCKS = [
  BLOCKS.PARAGRAPH,
  BLOCKS.HEADING_1,
  BLOCKS.HEADING_2,
  BLOCKS.HEADING_3,
  BLOCKS.HEADING_4,
  BLOCKS.HEADING_5,
  BLOCKS.HEADING_6,
  BLOCKS.OL_LIST,
  BLOCKS.UL_LIST,
  BLOCKS.HR,
  BLOCKS.QUOTE,
  BLOCKS.EMBEDDED_ENTRY,
  BLOCKS.EMBEDDED_ASSET,
  BLOCKS.EMBEDDED_RESOURCE
];
var TABLE_BLOCKS = [
  BLOCKS.TABLE,
  BLOCKS.TABLE_ROW,
  BLOCKS.TABLE_CELL,
  BLOCKS.TABLE_HEADER_CELL
];
var VOID_BLOCKS = [
  BLOCKS.HR,
  BLOCKS.EMBEDDED_ENTRY,
  BLOCKS.EMBEDDED_ASSET,
  BLOCKS.EMBEDDED_RESOURCE
];
var CONTAINERS = {
  [BLOCKS.OL_LIST]: [
    BLOCKS.LIST_ITEM
  ],
  [BLOCKS.UL_LIST]: [
    BLOCKS.LIST_ITEM
  ],
  [BLOCKS.LIST_ITEM]: LIST_ITEM_BLOCKS,
  [BLOCKS.QUOTE]: [
    BLOCKS.PARAGRAPH
  ],
  [BLOCKS.TABLE]: [
    BLOCKS.TABLE_ROW
  ],
  [BLOCKS.TABLE_ROW]: [
    BLOCKS.TABLE_CELL,
    BLOCKS.TABLE_HEADER_CELL
  ],
  [BLOCKS.TABLE_CELL]: [
    BLOCKS.PARAGRAPH,
    BLOCKS.UL_LIST,
    BLOCKS.OL_LIST
  ],
  [BLOCKS.TABLE_HEADER_CELL]: [
    BLOCKS.PARAGRAPH
  ]
};
var HEADINGS = [
  BLOCKS.HEADING_1,
  BLOCKS.HEADING_2,
  BLOCKS.HEADING_3,
  BLOCKS.HEADING_4,
  BLOCKS.HEADING_5,
  BLOCKS.HEADING_6
];
var TEXT_CONTAINERS = [
  BLOCKS.PARAGRAPH,
  ...HEADINGS
];
var V1_NODE_TYPES = [
  BLOCKS.DOCUMENT,
  BLOCKS.PARAGRAPH,
  BLOCKS.HEADING_1,
  BLOCKS.HEADING_2,
  BLOCKS.HEADING_3,
  BLOCKS.HEADING_4,
  BLOCKS.HEADING_5,
  BLOCKS.HEADING_6,
  BLOCKS.OL_LIST,
  BLOCKS.UL_LIST,
  BLOCKS.LIST_ITEM,
  BLOCKS.HR,
  BLOCKS.QUOTE,
  BLOCKS.EMBEDDED_ENTRY,
  BLOCKS.EMBEDDED_ASSET,
  INLINES.HYPERLINK,
  INLINES.ENTRY_HYPERLINK,
  INLINES.ASSET_HYPERLINK,
  INLINES.EMBEDDED_ENTRY,
  "text"
];
var V1_MARKS = [
  MARKS.BOLD,
  MARKS.CODE,
  MARKS.ITALIC,
  MARKS.UNDERLINE
];

// node_modules/@contentful/rich-text-types/dist/esm/emptyDocument.mjs
var EMPTY_DOCUMENT = {
  nodeType: BLOCKS.DOCUMENT,
  data: {},
  content: [
    {
      nodeType: BLOCKS.PARAGRAPH,
      data: {},
      content: [
        {
          nodeType: "text",
          value: "",
          marks: [],
          data: {}
        }
      ]
    }
  ]
};

// node_modules/@contentful/rich-text-types/dist/esm/helpers.mjs
var helpers_exports = {};
__export(helpers_exports, {
  isBlock: () => isBlock,
  isEmptyParagraph: () => isEmptyParagraph,
  isInline: () => isInline,
  isText: () => isText,
  stripEmptyTrailingParagraphFromDocument: () => stripEmptyTrailingParagraphFromDocument
});
function hasValue(obj, value) {
  for (const key of Object.keys(obj)) {
    if (value === obj[key]) {
      return true;
    }
  }
  return false;
}
function isInline(node) {
  return hasValue(INLINES, node.nodeType);
}
function isBlock(node) {
  return hasValue(BLOCKS, node.nodeType);
}
function isText(node) {
  return node.nodeType === "text";
}
function isEmptyParagraph(node) {
  if (node.nodeType !== BLOCKS.PARAGRAPH) {
    return false;
  }
  if (node.content.length !== 1) {
    return false;
  }
  const textNode = node.content[0];
  return textNode.nodeType === "text" && textNode.value === "";
}
function isValidDocument(document) {
  return document != null && typeof document === "object" && "content" in document && Array.isArray(document.content);
}
var MIN_NODES_FOR_STRIPPING = 2;
function stripEmptyTrailingParagraphFromDocument(document) {
  if (!isValidDocument(document) || document.content.length < MIN_NODES_FOR_STRIPPING) {
    return document;
  }
  const lastNode = document.content[document.content.length - 1];
  if (isEmptyParagraph(lastNode)) {
    return {
      ...document,
      content: document.content.slice(0, -1)
    };
  }
  return document;
}

// node_modules/@contentful/rich-text-types/dist/esm/validator/errors.mjs
var typeMismatchError = ({ path, property, typeName, value }) => {
  return {
    details: `The type of "${property}" is incorrect, expected type: ${typeName}`,
    name: "type",
    path: path.toArray(),
    type: typeName,
    value
  };
};
var minSizeError = ({ min, value, path }) => {
  return {
    name: "size",
    min,
    path: path.toArray(),
    details: `Size must be at least ${min}`,
    value
  };
};
var maxSizeError = ({ max, value, path }) => {
  return {
    name: "size",
    max,
    path: path.toArray(),
    details: `Size must be at most ${max}`,
    value
  };
};
var enumError = ({ expected, value, path }) => {
  return {
    details: `Value must be one of expected values`,
    name: "in",
    expected: [
      ...expected
    ].sort(),
    path: path.toArray(),
    value
  };
};
var unknownPropertyError = ({ property, path }) => {
  return {
    details: `The property "${property}" is not expected`,
    name: "unexpected",
    path: path.toArray()
  };
};
var requiredPropertyError = ({ property, path }) => {
  return {
    details: `The property "${property}" is required here`,
    name: "required",
    path: path.toArray()
  };
};

// node_modules/@contentful/rich-text-types/dist/esm/validator/assert.mjs
var ObjectAssertion = class {
  constructor(obj, path) {
    __publicField(this, "obj");
    __publicField(this, "path");
    __publicField(this, "_errors", []);
    __publicField(this, "catch", (...errors) => {
      this._errors.push(...errors);
    });
    __publicField(this, "exists", (key) => {
      if (key in this.obj) {
        return true;
      }
      this.catch(requiredPropertyError({
        property: key,
        path: this.path.of(key)
      }));
      return false;
    });
    __publicField(this, "object", (key) => {
      const value = key ? this.obj[key] : this.obj;
      if (key) {
        if (!this.exists(key)) {
          return false;
        }
      }
      if (typeof value === "object" && !Array.isArray(value) && value !== null) {
        return true;
      }
      const path = key ? this.path.of(key) : this.path;
      const property = key ?? this.path.last() ?? "value";
      this.catch(typeMismatchError({
        typeName: "Object",
        property,
        path,
        value
      }));
      return false;
    });
    __publicField(this, "string", (key) => {
      const value = this.obj[key];
      if (key && !this.exists(key)) {
        return false;
      }
      if (typeof value === "string") {
        return true;
      }
      this.catch(typeMismatchError({
        typeName: "String",
        property: key,
        path: this.path.of(key),
        value
      }));
      return false;
    });
    __publicField(this, "number", (key, optional) => {
      const value = this.obj[key];
      if (optional && !(key in this.obj)) {
        return true;
      }
      if (!this.exists(key)) {
        return false;
      }
      if (typeof value === "number" && !Number.isNaN(value)) {
        return true;
      }
      this.catch(typeMismatchError({
        typeName: "Number",
        property: key,
        path: this.path.of(key),
        value
      }));
      return false;
    });
    __publicField(this, "array", (key) => {
      const value = this.obj[key];
      if (key && !this.exists(key)) {
        return false;
      }
      if (Array.isArray(value)) {
        return true;
      }
      this.catch(typeMismatchError({
        typeName: "Array",
        property: key,
        path: this.path.of(key),
        value
      }));
      return false;
    });
    __publicField(this, "enum", (key, expected) => {
      const value = this.obj[key];
      if (typeof value === "string" && expected.includes(value)) {
        return true;
      }
      this.catch(enumError({
        expected,
        value,
        path: this.path.of(key)
      }));
      return false;
    });
    __publicField(this, "empty", (key) => {
      if (!this.array(key)) {
        return false;
      }
      const value = this.obj[key];
      if (value.length === 0) {
        return true;
      }
      this.catch(maxSizeError({
        max: 0,
        value,
        path: this.path.of(key)
      }));
      return false;
    });
    __publicField(this, "minLength", (key, min) => {
      if (!this.array(key)) {
        return false;
      }
      const value = this.obj[key];
      if (value.length >= min) {
        return true;
      }
      this.catch(minSizeError({
        min,
        value,
        path: this.path.of(key)
      }));
      return false;
    });
    __publicField(this, "noAdditionalProperties", (properties) => {
      const unknowns = Object.keys(this.obj).sort().filter((key) => !properties.includes(key));
      unknowns.forEach((property) => this.catch(unknownPropertyError({
        property,
        path: this.path.of(property)
      })));
      return unknowns.length === 0;
    });
    __publicField(this, "each", (key, assert2) => {
      if (!this.array(key)) {
        return;
      }
      const value = this.obj[key];
      let foundErrors = false;
      value.forEach((item, index) => {
        if (foundErrors) {
          return;
        }
        const errors = assert2(item, this.path.of(key).of(index));
        if (errors.length > 0) {
          foundErrors = true;
        }
        this.catch(...errors);
      });
    });
    this.obj = obj;
    this.path = path;
  }
  get errors() {
    const serializeError = (error) => JSON.stringify({
      details: error.details,
      path: error.path
    });
    return this._errors.filter((error, index) => this._errors.findIndex((step) => serializeError(error) === serializeError(step)) === index);
  }
};

// node_modules/@contentful/rich-text-types/dist/esm/validator/node.mjs
var VOID_CONTENT = [];
var NodeAssertion = class {
  constructor(contentRule, validateData) {
    __publicField(this, "contentRule");
    __publicField(this, "validateData");
    this.contentRule = contentRule;
    this.validateData = validateData;
  }
  assert(node, path) {
    var _a;
    const $ = new ObjectAssertion(node, path);
    if (!$.object()) {
      return $.errors;
    }
    $.noAdditionalProperties([
      "nodeType",
      "data",
      "content"
    ]);
    const { nodeTypes, min = 0 } = Array.isArray(this.contentRule) ? {
      nodeTypes: this.contentRule
    } : this.contentRule(node, path);
    if (nodeTypes.length === 0 && min > 0) {
      throw new Error(`Invalid content rule. Cannot have enforce a 'min' of ${min} with no nodeTypes`);
    }
    $.minLength("content", min);
    if (nodeTypes.length === 0) {
      $.empty("content");
    } else {
      $.each("content", (item, path2) => {
        const item$ = new ObjectAssertion(item, path2);
        if (!item$.object()) {
          return item$.errors;
        }
        item$.enum("nodeType", nodeTypes);
        return item$.errors;
      });
    }
    if ($.object("data")) {
      const dataErrors = ((_a = this.validateData) == null ? void 0 : _a.call(this, node.data, path.of("data"))) ?? [];
      $.catch(...dataErrors);
    }
    return $.errors;
  }
};
var EntityLinkAssertion = class extends NodeAssertion {
  constructor(linkType, contentNodeTypes) {
    super(contentNodeTypes, (data, path) => this.assertLink(data, path));
    __publicField(this, "linkType");
    __publicField(this, "type");
    __publicField(this, "assertLink", (data, path) => {
      const $ = new ObjectAssertion(data, path);
      if ($.object("target")) {
        const sys$ = new ObjectAssertion(data.target.sys, path.of("target").of("sys"));
        if (sys$.object()) {
          sys$.enum("type", [
            this.type
          ]);
          sys$.enum("linkType", [
            this.linkType
          ]);
          if (this.type === "Link") {
            sys$.string("id");
            sys$.noAdditionalProperties([
              "type",
              "linkType",
              "id"
            ]);
          } else if (this.type === "ResourceLink") {
            sys$.string("urn");
            sys$.noAdditionalProperties([
              "type",
              "linkType",
              "urn"
            ]);
          }
        }
        $.catch(...sys$.errors);
      }
      $.noAdditionalProperties([
        "target"
      ]);
      return $.errors;
    });
    this.linkType = linkType;
    this.type = this.linkType.startsWith("Contentful:") ? "ResourceLink" : "Link";
  }
};
var HyperLinkAssertion = class extends NodeAssertion {
  constructor() {
    super([
      "text"
    ], (data, path) => this.assertLink(data, path));
    __publicField(this, "assertLink", (data, path) => {
      const $ = new ObjectAssertion(data, path);
      $.string("uri");
      $.noAdditionalProperties([
        "uri"
      ]);
      return $.errors;
    });
  }
};
var assert = (contentRule, validateData) => {
  return new NodeAssertion(contentRule, validateData);
};
var assertLink = (linkType, contentRule) => {
  return new EntityLinkAssertion(linkType, contentRule);
};

// node_modules/@contentful/rich-text-types/dist/esm/validator/index.mjs
var assertInlineOrText = assert([
  ...Object.values(INLINES),
  "text"
].sort());
var assertList = assert([
  BLOCKS.LIST_ITEM
]);
var assertVoidEntryLink = assertLink("Entry", VOID_CONTENT);
var assertTableCell = assert(() => ({
  nodeTypes: [
    BLOCKS.PARAGRAPH
  ],
  min: 1
}), (data, path) => {
  const $ = new ObjectAssertion(data, path);
  $.noAdditionalProperties([
    "colspan",
    "rowspan"
  ]);
  $.number("colspan", true);
  $.number("rowspan", true);
  return $.errors;
});
var nodeValidator = {
  [BLOCKS.DOCUMENT]: assert(TOP_LEVEL_BLOCKS),
  [BLOCKS.PARAGRAPH]: assertInlineOrText,
  [BLOCKS.HEADING_1]: assertInlineOrText,
  [BLOCKS.HEADING_2]: assertInlineOrText,
  [BLOCKS.HEADING_3]: assertInlineOrText,
  [BLOCKS.HEADING_4]: assertInlineOrText,
  [BLOCKS.HEADING_5]: assertInlineOrText,
  [BLOCKS.HEADING_6]: assertInlineOrText,
  [BLOCKS.QUOTE]: assert(CONTAINERS[BLOCKS.QUOTE]),
  [BLOCKS.EMBEDDED_ENTRY]: assertVoidEntryLink,
  [BLOCKS.EMBEDDED_ASSET]: assertLink("Asset", VOID_CONTENT),
  [BLOCKS.EMBEDDED_RESOURCE]: assertLink("Contentful:Entry", VOID_CONTENT),
  [BLOCKS.HR]: assert(VOID_CONTENT),
  [BLOCKS.OL_LIST]: assertList,
  [BLOCKS.UL_LIST]: assertList,
  [BLOCKS.LIST_ITEM]: assert([
    ...LIST_ITEM_BLOCKS
  ].sort()),
  [BLOCKS.TABLE]: assert(() => ({
    nodeTypes: [
      BLOCKS.TABLE_ROW
    ],
    min: 1
  })),
  [BLOCKS.TABLE_ROW]: assert(() => ({
    nodeTypes: [
      BLOCKS.TABLE_CELL,
      BLOCKS.TABLE_HEADER_CELL
    ],
    min: 1
  })),
  [BLOCKS.TABLE_CELL]: assertTableCell,
  [BLOCKS.TABLE_HEADER_CELL]: assertTableCell,
  [INLINES.HYPERLINK]: new HyperLinkAssertion(),
  [INLINES.EMBEDDED_ENTRY]: assertVoidEntryLink,
  [INLINES.EMBEDDED_RESOURCE]: assertLink("Contentful:Entry", VOID_CONTENT),
  [INLINES.ENTRY_HYPERLINK]: assertLink("Entry", [
    "text"
  ]),
  [INLINES.ASSET_HYPERLINK]: assertLink("Asset", [
    "text"
  ]),
  [INLINES.RESOURCE_HYPERLINK]: assertLink("Contentful:Entry", [
    "text"
  ])
};

// node_modules/@contentful/rich-text-react-renderer/dist/rich-text-react-renderer.esm.js
function appendKeyToValidElement(element, key) {
  if ((0, import_react.isValidElement)(element) && element.key === null) {
    return (0, import_react.cloneElement)(element, {
      key
    });
  }
  return element;
}
function nodeListToReactComponents(nodes, options) {
  return nodes.map((node, index) => {
    return appendKeyToValidElement(nodeToReactComponent(node, options), index);
  });
}
function nodeToReactComponent(node, options) {
  const { renderNode, renderMark, renderText, preserveWhitespace } = options;
  if (helpers_exports.isText(node)) {
    let nodeValue = renderText ? renderText(node.value) : node.value;
    if (preserveWhitespace && !renderText) {
      nodeValue = nodeValue.replace(/ {2,}/g, (match) => " ".repeat(match.length));
      const lines = nodeValue.split("\n");
      const jsxLines = [];
      lines.forEach((line, index) => {
        jsxLines.push(line);
        if (index !== lines.length - 1) {
          jsxLines.push(import_react.default.createElement("br", null));
        }
      });
      nodeValue = jsxLines;
    }
    return node.marks.reduce((value, mark) => {
      if (!renderMark[mark.type]) {
        return value;
      }
      return renderMark[mark.type](value);
    }, nodeValue);
  } else {
    const children = nodeListToReactComponents(node.content, options);
    if (!node.nodeType || !renderNode[node.nodeType]) {
      return import_react.default.createElement(import_react.default.Fragment, null, children);
    }
    return renderNode[node.nodeType](node, children);
  }
}
var defaultNodeRenderers = {
  [BLOCKS.DOCUMENT]: (node, children) => children,
  [BLOCKS.PARAGRAPH]: (node, children) => import_react.default.createElement("p", null, children),
  [BLOCKS.HEADING_1]: (node, children) => import_react.default.createElement("h1", null, children),
  [BLOCKS.HEADING_2]: (node, children) => import_react.default.createElement("h2", null, children),
  [BLOCKS.HEADING_3]: (node, children) => import_react.default.createElement("h3", null, children),
  [BLOCKS.HEADING_4]: (node, children) => import_react.default.createElement("h4", null, children),
  [BLOCKS.HEADING_5]: (node, children) => import_react.default.createElement("h5", null, children),
  [BLOCKS.HEADING_6]: (node, children) => import_react.default.createElement("h6", null, children),
  [BLOCKS.EMBEDDED_ENTRY]: (node, children) => import_react.default.createElement("div", null, children),
  [BLOCKS.EMBEDDED_RESOURCE]: (node, children) => import_react.default.createElement("div", null, children),
  [BLOCKS.UL_LIST]: (node, children) => import_react.default.createElement("ul", null, children),
  [BLOCKS.OL_LIST]: (node, children) => import_react.default.createElement("ol", null, children),
  [BLOCKS.LIST_ITEM]: (node, children) => import_react.default.createElement("li", null, children),
  [BLOCKS.QUOTE]: (node, children) => import_react.default.createElement("blockquote", null, children),
  [BLOCKS.HR]: () => import_react.default.createElement("hr", null),
  [BLOCKS.TABLE]: (node, children) => import_react.default.createElement("table", null, import_react.default.createElement("tbody", null, children)),
  [BLOCKS.TABLE_ROW]: (node, children) => import_react.default.createElement("tr", null, children),
  [BLOCKS.TABLE_HEADER_CELL]: (node, children) => import_react.default.createElement("th", null, children),
  [BLOCKS.TABLE_CELL]: (node, children) => import_react.default.createElement("td", null, children),
  [INLINES.ASSET_HYPERLINK]: (node) => defaultInline(INLINES.ASSET_HYPERLINK, node),
  [INLINES.ENTRY_HYPERLINK]: (node) => defaultInline(INLINES.ENTRY_HYPERLINK, node),
  [INLINES.RESOURCE_HYPERLINK]: (node) => defaultInlineResource(INLINES.RESOURCE_HYPERLINK, node),
  [INLINES.EMBEDDED_ENTRY]: (node) => defaultInline(INLINES.EMBEDDED_ENTRY, node),
  [INLINES.EMBEDDED_RESOURCE]: (node, _children) => defaultInlineResource(INLINES.EMBEDDED_RESOURCE, node),
  [INLINES.HYPERLINK]: (node, children) => import_react.default.createElement("a", {
    href: node.data.uri
  }, children)
};
var defaultMarkRenderers = {
  [MARKS.BOLD]: (text) => import_react.default.createElement("b", null, text),
  [MARKS.ITALIC]: (text) => import_react.default.createElement("i", null, text),
  [MARKS.UNDERLINE]: (text) => import_react.default.createElement("u", null, text),
  [MARKS.CODE]: (text) => import_react.default.createElement("code", null, text),
  [MARKS.SUPERSCRIPT]: (text) => import_react.default.createElement("sup", null, text),
  [MARKS.SUBSCRIPT]: (text) => import_react.default.createElement("sub", null, text),
  [MARKS.STRIKETHROUGH]: (text) => import_react.default.createElement("s", null, text)
};
function defaultInline(type, node) {
  return import_react.default.createElement("span", {
    key: node.data.target.sys.id
  }, "type: ", node.nodeType, " id: ", node.data.target.sys.id);
}
function defaultInlineResource(type, node) {
  return import_react.default.createElement("span", {
    key: node.data.target.sys.urn
  }, "type: ", node.nodeType, " urn: ", node.data.target.sys.urn);
}
function documentToReactComponents(richTextDocument, options = {}) {
  if (!richTextDocument) {
    return null;
  }
  let processedDocument = richTextDocument;
  if (options.stripEmptyTrailingParagraph) {
    processedDocument = helpers_exports.stripEmptyTrailingParagraphFromDocument(richTextDocument);
  }
  return nodeToReactComponent(processedDocument, {
    renderNode: {
      ...defaultNodeRenderers,
      ...options.renderNode
    },
    renderMark: {
      ...defaultMarkRenderers,
      ...options.renderMark
    },
    renderText: options.renderText,
    preserveWhitespace: options.preserveWhitespace
  });
}
export {
  documentToReactComponents
};
//# sourceMappingURL=@contentful_rich-text-react-renderer.js.map
